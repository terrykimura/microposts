<div class="col-md-8">
    <h3><%= @user.name %> <%= @name %> (<%= @followlist.count %>)</h3>
    <ul class="users follow">
        <% @followlist.each do |f| %>
            <li class="clearfix"><%= f.name %></li>
        <% end %>
    </ul>
</div>